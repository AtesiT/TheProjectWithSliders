import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello, world!")
                .font(.largeTitle)
            Spacer()
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
